# HepG2 ATAC ChromBPNet model

Released ENCODE tier-1 HepG2 ATAC ChromBPNet model. **Not trained here — downloaded.**

Fill in when fetched:
- Source / accession:
- Fold / chromosome split used by the release:
- Bias model:
- SHA / date downloaded:

Needed for the assay-concordance analysis (`src/assay_concordance.py`): the novel question is
whether DNase and ATAC models agree on *counterfactual variant* predictions, not just on reference
accessibility.
